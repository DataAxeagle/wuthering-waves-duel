# 项目文档治理规则更新

- 新增 `rules/项目协作规则.md`。
- 固化“所有更新必须先备份，再制作新版本”。
- 固化每个新 AI 对话或 Agent 进入项目时，先读 `PROJECT.md`，再完整读取 `rules/` 全部 Markdown。
- 用户在对话中表达明确且可复用的限制或强制要求时，本轮结束前写入合适的规则文件。
- 明确 `PROJECT.md`、`rules/`、`changes/` 的职责分工。
- `changes/` 改为按需读取：需要了解历史内容，或任务明确涉及过往变更时才读取相关记录。
