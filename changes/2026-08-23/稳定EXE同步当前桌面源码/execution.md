# 执行记录

1. 检查桌面分享包后确认其最新版本仍为 `20260820-215325`。
2. 在当前项目桌面源码 `demo/` 中定位到 2026-08-23 的更新，并与分享包基线做哈希对比。
3. 确认仅 `core.js`、`game.js`、`styles.css` 发生变化。
4. 使用 `20260820-215325` 稳定 EXE 包作为基线，替换上述三个文件并输出新的带时间戳 ZIP。
5. 执行源码单测、成品文件哈希核验和包内 JavaScript 语法检查。
