# 对局内调整画面大小返回路径修复

- 从对局主菜单进入“调整画面大小”后，设置页新增“关闭调整，返回当前对局”。
- 此路径下的“← 返回封面”会显示为“← 返回当前对局”，点击同样恢复原对局。
- 从首页进入普通设置时仍保持“← 返回封面”行为。
- 本轮先备份 `mobile/` 到 `archives/backups/mobile-in-game-scale-return-20260825-154125/`，再生成完整/精简包并发布 Cloudflare。
