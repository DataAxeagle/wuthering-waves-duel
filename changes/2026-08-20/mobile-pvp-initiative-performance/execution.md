# 执行记录

1. 审计准备阶段发现：服务端已限制只有 `coinWinner` 可提交先后手，但规则核心首次选择后立即锁死，不能满足“确认前修改”。
2. 修改 `chooseInitiative`：决定方确认准备前可重复切换；确认后锁定。非决定方提交仍由权威服务端拒绝。
3. 更新原战场准备弹窗：决定方显示可修改按钮和蓝色提示；非决定方隐藏按钮并显示由对方选择后推导出的自身顺序。
4. 审计同步链路发现：操作响应已包含最新脱敏视图，前端仍额外调用 `state`；Realtime 更新也被忽略并再次调用 `state`。
5. 新增 `applyViewRow`：操作方直接应用指令响应，对手直接应用 `pvp_player_views` Realtime 新行；只有缺失或异常时完整刷新。
6. 使用无第三方依赖的 `scripts/optimize-mobile-card-assets.mjs` 将 52 张当前在线 PNG 转为最长边 1200px、质量 0.84 的 WebP，并机械更新手机版 `catalog.js/json`。
7. 原 PNG 68,392,067 字节，WebP 5,371,200 字节，减少 63,020,867 字节（92.15%）。抽查文字与卡面清晰度正常。
8. 51 项规则、PVP、安全与资源测试通过；Deno Edge Function 类型检查通过。
9. `844×390`、`667×375` PVP 大厅以及 `844×390` 先后手蓝框布局无溢出、遮挡或裁切。
10. 已部署 Supabase `pvp` Edge Function，并发布 Cloudflare Pages 正式版本 `09e42eec`。
11. 正式站再次通过 7 个基础视口、2 个 PVP 手机横屏视口和 1 个先后手提示视口回归；截图保存到工作区 `output/测试/2026-08-20/`。
12. 正式站 6 张真实卡牌无缓存抽样均返回正确图片类型和缓存头：WebP 682,462 字节、约 998ms，PNG 8,658,774 字节、约 6,028ms；体积减少 92.12%。
13. 生成新的手机版分享包与日期归档，均含 246 个文件且 SHA-256 一致：`BB3F175585C1510910B4934C2635BA9711A0A5C85008F55F48C18BB9CDA60978`。
