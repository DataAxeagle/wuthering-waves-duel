# 执行记录

1. 备份旧版 `mobile/`；第一次命令因 `-LiteralPath` 不展开通配符，只留下空目录 `backups/mobile-20260820-081718/`，未改动源码。第二次备份 `mobile-20260820-081735/` 完整通过哈希核验。
2. 为 `DuelGame` 增加多人准备模式、规则版本和可持久化随机状态；默认单人行为保持不变。
3. 新增 Supabase migration、RLS、私有 Realtime 授权、事务提交、席位恢复和定时清理。
4. 新增 PVP Edge Function，动作使用 `expectedVersion + actionId` 做并发与幂等控制。
5. 新增 `pvp.html`、`pvp.js`、`pvp.css` 和公开配置；主菜单增加好友 PVP 入口。首次实现的独立简化战斗页不符合产品边界，已在备份后移除；大厅开局现在跳转到原 `index.html?pvp=1` 战场。
6. 新增隐藏信息投影及测试，确认对手手牌、牌库、角色牌库和暗置角色不会进入另一方视图。
7. 使用 Deno 真实解析 Supabase npm 依赖并完成类型检查。
8. 使用项目 `browser-automation` CDP 环境进行 iPhone 横屏回归；依据首轮截图修复卡面路径和战场溢出。
9. 用户完成 Supabase CLI 登录后，创建独立项目 `pgxfrxrrcumavalbwqse`，推送迁移、部署 `pvp` Edge Function，启用匿名登录与 Turnstile；服务端密钥未写入项目文件。
10. 生成 `鸣潮对决-手机版-20260820-091507.zip` 并复制到 `output/导出/2026-08-20/手机版/`；两个 ZIP 均为 194 个条目，SHA-256 完全一致。
11. 安全审查发现并修复：暗置行动区泄露、房间开局/终局非原子、结束后仍可提交动作、准备取消竞态、重复动作旧视图、恢复/房间锁死锁和内部成员字段直读。
12. 最终 Deno 编译、46 项测试、7 视口检查全部通过；重新生成最终包 `鸣潮对决-手机版-20260820-093808.zip`，并完成手机版独立归档与 SHA-256 核验。
13. 新增 `mobile/pvp-game.js` 作为原战场的联网适配层；`mobile/game.js` 在 PVP 参数下发送权威命令并关闭 AI 调度，非 PVP 路径保持原逻辑。玩家视图按席位本地化，使自己的角色、手牌和操作区始终位于原战场下方。
14. 使用两个隔离浏览器会话完成线上关键流程联调；修复响应阶段“不放牌”按钮显示和心跳瞬时失败提示。最终 47 项测试、7 视口检查及 Deno 类型检查通过。
15. 重新部署 Edge Function 和 Cloudflare Pages 正式版本 `23e338d7`；双方重载后仍为原单机战场，回合、血量、手牌数和追击状态一致，30 秒心跳内无错误提示。
16. 按手机版交付规则创建 `archives/backups/mobile-20260820-111738/`，194 个文件逐一 SHA-256 核验无差异；生成并单独归档 `鸣潮对决-手机版-20260820-111738.zip`，两个 ZIP 均为 194 个条目且 SHA-256 为 `D545BD37166438075420B1BEE1961463D11C27FC92498712B960B704A37F2DF2`。
