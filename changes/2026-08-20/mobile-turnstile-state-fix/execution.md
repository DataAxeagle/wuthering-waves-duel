# 执行记录

1. 正式站复现确认：Turnstile 外部脚本可能延迟约 10 秒，原代码在 token 产生前已开放房间按钮，且只有成功回调，没有错误、过期、超时或浏览器不支持处理。
2. 在 PVP 大厅新增验证状态文本与重试按钮，房间按钮在 HTML 初始状态即为禁用。
3. 新增 Turnstile 加载等待、15 秒脚本超时、错误回调、token 过期回调、交互超时回调和不支持回调。
4. 成功获得 token 后才开放按钮；匿名登录消费 token 后标记为“身份已验证”。匿名登录失败时重新锁定按钮并要求重试。
5. 初始化时先读取 Supabase 现有会话，已有有效匿名身份则跳过重复验证。
6. 新增静态回归测试，确认按钮默认锁定、状态节点、重试入口和全部错误回调存在。
7. 48 项玩法/PVP 测试通过；`844×390`、`667×375` PVP 横屏布局无溢出、重叠或裁切。
8. 发布 Cloudflare Pages 版本 `7e43cbbe`；正式站实测加载阶段按钮禁用，约 10 秒后验证框出现且按钮仍保持禁用，等待真实用户完成验证。
9. 生成 `鸣潮对决-手机版-20260820-120712.zip` 并复制到手机版日期归档；两个 ZIP 均为 194 个条目、279,938,193 字节，SHA-256 为 `87EDDEDF35DA23494D88185E5FE1053BABC0473DD606E6CE76CAD38EB80B3789`。
