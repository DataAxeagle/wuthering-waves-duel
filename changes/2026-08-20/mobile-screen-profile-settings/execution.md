# 执行记录

1. 使用 Apple、Samsung、小米官方规格核对当前主流手机的屏幕尺寸、物理分辨率和常见长宽比。
2. 选择 16:9、19.5:9、20:9 三档横屏设计视口，并保留自动推荐入口。
3. 在设置页新增可触摸选择卡、当前视口提示和已选档位状态。
4. 使用 `localStorage` 键 `waves-duel-screen-profile-v1` 保存选择；窗口尺寸或方向变化时重新计算自动档位。
5. 以 CSS 自定义变量调整战场关键尺寸，避免整页缩放导致点击坐标、弹窗定位和安全区失真。
6. 新增 `tests/check-screen-profile-layout.mjs`，验证自动推荐、手动切换、本地保存、设置页裁切和战场实际尺寸。
7. 736 × 414、852 × 393、915 × 412 三档浏览器回归全部通过，无水平溢出、设置项裁切或战场越界。
8. 完整 55 项规则、PVP、安全与资源测试通过；JavaScript 语法与 `git diff --check` 通过。
9. 生成手机版正式包与日期归档，逐文件核对 248 个文件无缺失、无额外项，两个 ZIP 的 SHA-256 一致。
10. 使用 Wrangler 4.124.0 部署 Cloudflare Pages 正式版本 `ef7faa3d`；247 个资源完成核对，其中 6 个更新、241 个复用，Functions 编译与上传成功。
11. 稳定域名首页、PVP 页面、`/api/status`、`game.js` 和 `styles.css` 均返回 200；线上代码确认包含屏幕适配及停止追击修复。
12. 在稳定生产域名再次执行三档浏览器回归，自动推荐、手动切换、本地恢复和布局边界全部通过。
