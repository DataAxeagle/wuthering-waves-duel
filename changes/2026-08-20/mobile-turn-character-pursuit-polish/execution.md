# 执行记录

1. 审计确认旧版女漂泊者 Lv.2 会抽牌，但发生在常规回合抽牌之后，且没有回合开始角色动画元数据。
2. 调整 `startTurn`：先结算 `triggerTurnStart`，再抽取首回合 1 张或普通回合 2 张；快照同步保存回合开始效果与常规抽牌元数据。
3. 新增顺序化回合展示，确保回合提示、角色触发、技能抽牌和常规抽牌依次播放。
4. 补齐今汐连击增伤与炽霞专属领队技增伤的角色触发事件，并在追击伤害前播放角色弹窗。
5. 将我方附件原 PNG 原样复制为 `mobile/assets/backgrounds/turn-transition-frame.png`，SHA-256 为 `9D83D6BFCCDA1936A96ECC31FF38B2C0B2BA250E0EE274B34976B7B8D8FDE60E`；将对方附件原 PNG 原样复制为 `mobile/assets/backgrounds/turn-transition-opponent.png`，SHA-256 为 `E0229FF461AD3286BD3D6CE33DCBB5A07D1C9563D6CF0F7A71E2D03FAB568076`。两组源文件与项目文件哈希均一致。
6. 新增追击提示状态：每次追击后重新计算合法红牌；只要仍有可用红牌就持续高亮追击，仅在没有合法红牌时高亮停止；阶段结束后自动清除。
7. 53 项规则、PVP、安全与资源测试通过，JavaScript 语法检查通过。
8. 844 × 390 视觉回归通过：我方与对方回合原图均以 `<img>` 直接缩放居中且未裁切；角色效果框和右侧停止追击按钮也未裁切。
9. 按用户要求停止在本地源码与测试阶段，没有打包、上传或部署。
10. 删除首页“全屏游玩”按钮、Fullscreen API 函数和点击监听；补齐电脑横屏预览的首页紧凑样式，防止顶部文字裁切。
11. 按用户纠正移除回合图上的深色遮罩、动态文字、调色与滤镜；我方固定使用第二张素材，对方固定使用第一张素材。
12. 修复停止追击按钮：新增独立 `playerPursuit` 阶段状态，文案不再被 `uiLocked` / `aiRunning` 临时锁误判；追击教学时把停止按钮提升到遮罩上层保持可见。
13. 在 844 × 390 手机横屏完成用户截图与修复结果并排回归，停止按钮显示正确且没有裁切。
