# 验证记录

- `node --check demo/game.js` 通过。
- `node --check demo/core.js` 通过。
- `node --test tests/game.test.cjs`：25 通过，0 失败。
- `node --test tests/card-library.test.cjs`：3 通过，0 失败。
- 增加断言：抽后弃牌期间保持在 `effect-discard` 阶段和原回合方，确认弃牌后才切换对手回合。
- 增加断言：双方蓝色卡对抗时，双方依次抽牌、弃牌，且最后由原回合方结束回合。
- 更新战斗回归：无追击战斗结算后保持原回合方的 `post-battle` 状态，只有显式结束回合才移交对手。
- 弹窗确认按钮的 HTML hidden 属性与 CSS hidden 类同步切换，避免视觉可选但没有可用确认入口。
- 分享包完整性检查：包含启动脚本、游戏页面与 AI 决策记录说明。
- `node --test tests/game.test.cjs`：26 通过，0 失败；`node --test tests/card-library.test.cjs`：3 通过，0 失败。
- 最新分享包完整性检查：191 个条目，包含 `app/demo/game.js` 与 `app/demo/styles.css`。
- 教程逐步聚焦改动后，重复执行游戏逻辑 26 项与卡组库 3 项测试，均通过。
- 修正中央确认框定位回归后，重复执行游戏逻辑 26 项与卡组库 3 项测试，均通过。
- 加入追击条件教学与逐区场地导览后，重复执行游戏逻辑 26 项与卡组库 3 项测试，均通过。
- 删除结束回合教学并重构导览定位框后，重复执行游戏逻辑 26 项与卡组库 3 项测试，均通过。
- 取消会受 Windows 缩放影响的独立定位层后，重复执行游戏逻辑 26 项与卡组库 3 项测试，均通过。
- 为 `SD02-020/021` 增加卡面映射断言；卡组库 3 项与游戏规则 26 项测试均通过。
- 导览可见高亮与主界面对局关闭逻辑改动后，重复执行游戏逻辑 26 项与卡组库 3 项测试，均通过。
- 场地导览高亮状态修复后，重复执行游戏逻辑与卡组库测试；浏览器视觉检验未执行。
