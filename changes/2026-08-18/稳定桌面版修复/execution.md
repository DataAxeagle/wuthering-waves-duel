# 执行记录

1. 复核黑屏窗口：页面可访问性树存在，说明游戏 DOM 已加载；黑屏发生在 Electron 渲染合成层。
2. 对比原可用分享包，确认原路径为本地 Node 服务加浏览器渲染，且携带完整运行时与素材。
3. 新建 .NET 单文件启动器：后台启动包内 `node.exe server.js`，等待健康检查成功后以 `msedge.exe --app=http://127.0.0.1:<动态端口>` 打开无地址栏窗口。
4. 将上级目录当前 `demo/`、`server.js`、可携带 Node runtime 复制进稳定包。
5. 修复卡图 URL 的中文路径编码，并同步更新卡库回归断言。
6. 修复启动器将 Edge 作为唯一浏览器的兼容性问题：改为 Edge → Chrome → 系统默认浏览器的三级回退。
