# 执行记录

1. 修改 `core.js`：`respondContest(..., null)` 不再因存在合法牌而拒绝；补充 `handLimitPlayer` 并纳入存档。
2. 修改 `game.js`：响应窗始终显示弃权按钮；对抗、连击和 AI 流程统一处理手牌上限；支付弹窗在动画完成后再恢复等待流程。
3. 修改 `styles.css`：使用 `dvh` 与 `clamp()` 调整顶部栏、战场、角色、手牌和响应窗的尺寸与安全边距。
4. 增加两项规则测试：可用牌时主动弃权、手牌上限的安全回合推进。
5. 打包并按关键文件哈希核验分享包。
