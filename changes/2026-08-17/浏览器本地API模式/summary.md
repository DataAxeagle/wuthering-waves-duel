# 浏览器本地 API 模式

## 结论

直接双击 `打开游戏.html` 后，玩家可在游戏设置或首次引导中填写 DeepSeek API Key；Key 仅写入当前浏览器的 Local Storage，不进入对局存档、分享包或游戏日志。

## 边界

- 浏览器直连失败、超时或被跨域策略拦截时，AI 自动退回本地规则逻辑，游戏流程不会中断。
- 联网启动器模式仍保留，继续使用 Windows 用户凭据方式保存 Key。
- 本地浏览器存储适用于用户明确接受“本机本浏览器保存”的本地原型场景；同一浏览器配置文件的其他使用者可读取该 Key。
