# CMD 持久化与决策日志

## 结论

联网 CMD 模式成为唯一启动和 DeepSeek 模式：它会把玩家名称、累计战绩和局内存档写入当前 Windows 用户的数据目录，因此更新或替换分享包不会覆盖资料；每局 DeepSeek 决策另在游戏包根目录的 `AI决策记录/` 中生成独立 Markdown。

## 运行边界

- `启动游戏.cmd` 是唯一入口，未配置 Key 时自动使用本地规则 AI；`配置AI.cmd` 管理 DeepSeek Key。
- `AI决策记录/` 不由打包脚本预置或覆盖。若手动删除整个旧游戏文件夹，应先自行保留该文件夹。
