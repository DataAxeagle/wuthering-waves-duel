# 执行记录

1. 将行动牌渲染的卡图键从仅 `key` 改为 `key || id`，兼容测试场创建的卡牌实例。
2. 调整查看手牌弹窗的布局与按钮可见性。
3. 调整两个 VBS 启动入口：分享包优先启动 `游戏文件/launcher.ps1`，源码目录保留原路径兼容。
4. 调整分享包脚本，将全部运行文件放入 `游戏文件/`，根目录仅保留玩家入口和说明。
5. 打包并执行启动器只读自检。
