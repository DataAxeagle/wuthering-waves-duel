# 行动牌立绘与精简分享包

## 交付

- 测试场构造的行动牌改用卡牌 `id` 回查卡库立绘，因此手牌、中央翻牌、协奏区和查看手牌展示均会显示实际卡面。
- 查看手牌弹窗改为居中展示；卡面区可滚动，`确认并继续结算` 按钮固定可见并自动聚焦。
- 最新分享包根目录只保留：`启动游戏.vbs`、`配置 AI.vbs`、`分享说明.txt` 和内部目录 `游戏文件/`。
- 内部目录包含服务端、启动器、便携 Node 和游戏网页文件；已通过启动器自检。

## 验证

- `node --check demo/game.js`
- `node --test tests/game.test.cjs tests/card-library.test.cjs`：21/21 通过。
- 分享包启动器 `-ValidateOnly` 通过。
- 分享包与源码的 `game.js`、`styles.css`、两个 VBS 启动入口哈希一致。
