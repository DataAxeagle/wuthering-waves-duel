# 响应式缩放布局

## 结论

战场由固定最小宽度的桌面三栏，调整为桌面、紧凑桌面、窄屏三档布局。浏览器 Ctrl 缩放导致逻辑宽度下降时，窄屏会把操作区移到战场下方，避免其与战场或手牌相互覆盖。

## 布局规则

- 大于 1050px：保留三栏，全部列使用可收缩 `minmax(0, …)`。
- 720px 至 1050px：隐藏左侧信息栏，战场在上、操作区横向排在下、手牌继续置底。
- 小于等于 720px：取消遗留的 760px 最小宽度，操作区单列化，卡牌与中场进一步缩小。
- 低高度窗口取消 680px 强制最小高度，避免缩放后产生虚假可视区域。
