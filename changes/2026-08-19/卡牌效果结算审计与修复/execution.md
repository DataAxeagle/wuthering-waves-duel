# 执行记录

1. 以 `demo/card-library/catalog.json` 的角色与行动卡文本为准，核对 `demo/core.js` 的触发条件、数值计算和回合状态。
2. 将“各回合结束时”触发从单一结束回合方扩展为双方领队分别结算；回合开始触发仍仅对当前回合方结算。
3. 新增并验证：
   - 炽霞 Lv.2 对其领队技卡的伤害 `+3`；
   - 啾啾斗意被蓝色卡击败后返回己方手牌；
   - 朔风旋涌胜利后，使对方下一回合的红色卡费用 `+1`；
   - 秧秧 Lv.2 面对费用不足的对手时，直接产生 3 点待结算伤害。
4. 新增针对上述时机和边界的单元测试，并通过完整引擎与卡库测试。
