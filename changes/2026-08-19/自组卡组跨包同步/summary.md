# 自组卡组跨包同步

- 自组卡组加入 Windows 用户资料 `player-profile.json`，与玩家名称、战绩和局内存档统一同步。
- 保存或删除自组卡组后自动同步；在另一端口、新分享包或直接覆盖包文件后，启动游戏会恢复已同步的自组卡组。
- 兼容旧资料：若 Windows 用户资料尚无 `customDecks` 字段，保留浏览器当前卡组并在首次新版正常启动后补写。
- API Key 仍独立保存在 Windows 凭据管理器，未改变存储方式。
