# 角色效果动画顺序

## 结论

- 回合结束角色效果现在产生带角色名、卡面文本与触发时机的事件，界面会先播放效果说明和对应抽牌，再播放下一回合的常规抽牌。
- 今汐以红色行动卡对抗时的 1 点附加伤害，现会在首轮战斗效果展示后播放专属效果说明与伤害动画，再进入追击阶段。

## 验证

- `node --check .\\demo\\game.js` 通过。
- `node --test .\\tests\\game.test.cjs`：29 / 29 通过。
- `node --test .\\tests\\card-library.test.cjs`：3 / 3 通过。
- 自动浏览器视觉回归未完成：专用 Chrome 的 CDP 调试端口 `9222` 被本机 Chrome 进程拒绝；未关闭用户浏览器。

## 范围

- 修改 `demo/core.js`、`demo/game.js` 与对应引擎测试。
- 未改动卡面、卡牌文本、移动端副本、AI 服务和用户资料。
