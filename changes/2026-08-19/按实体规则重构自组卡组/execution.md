# 执行记录

1. 自组数据从 `heroIds` 改为 `roleCards`；运行时从已选角色卡推导 3 种角色与可升级角色牌库。
2. 旧保存记录无 `roleCards` 时，以旧 `heroIds` 自动补全该角色的既有角色卡，保留原可用牌组。
3. 构筑界面改为逐张展示、加入和移除角色卡；摘要同时显示角色卡总数和角色种类数。
4. 保存前与 `DuelGame` 创建对局前均校验同一套规则，防止浏览器存储被修改后绕过界面限制。
5. 新增回归：指定角色卡不自动补齐、缺少 Lv.0、第四种角色和未选角色专属行动卡均被拒绝。
