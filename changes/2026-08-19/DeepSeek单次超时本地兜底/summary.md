# DeepSeek 单次超时本地兜底

- DeepSeek 的单次决策等待上限由客户端 22 秒、服务端 20 秒统一收敛为 10 秒。
- 常规回合、响应与红色追击均复用同一决策函数，因此所有单次 DeepSeek 决策都会在超时后仅本次使用本地规则 AI。
- 超时不改变 AI 的长期模式；下一次独立决策仍优先调用 DeepSeek。
- 保留现有本地 AI 的合法选项与追击评分逻辑，未修改对局规则或流程。
