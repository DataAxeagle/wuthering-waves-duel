# 执行记录

- 新增横屏提示、`viewport-fit=cover`、触屏点击日志卡牌详情与横屏断点。
- 保留桌面布局；触屏横屏仅在窄屏粗指针设备生效。
- 新增 Cloudflare Pages 静态响应头，并更新项目与使用说明。
- 新增静态单机模式：Pages 域名跳过状态、心跳、用户资料、AI 决策等 `/api` 请求，并隐藏 DeepSeek 配置。
- 使用经 SHA-256 校验的 FFmpeg 9.0.1 临时构建，生成 3.58MiB 的 854×480 H.264/AAC 菜单视频；首帧画面与音视频流均已核验。Cloudflare Pages 稳定生产地址已部署并验证视频 MIME 类型为 `video/mp4`。
- 验证项：JavaScript 语法、核心规则测试、移动尺寸布局、横屏首屏触控目标、静态模式零 API 请求与触屏路径回归。

## 已知发布前项

- 联机服务端尚未实现；后续按“昵称 + 六位房间码 + 创建/加入/观战 + 服务端权威”独立接入，不与单机静态站混用。
